<% if @birth.errors.any? %>
  <div id="error_explanation">
    <h2>
      <%= pluralize(@birth.errors.count, "error") %> prohibited this birth from being saved:
    </h2>
    <ul>
      <% @birth.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= form_for @birth do |f| %>
  <p>
    <%= f.label :due_date %><br>
    <%= f.date_field :due_date %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
<%= link_to 'Cancel', births_path %>
